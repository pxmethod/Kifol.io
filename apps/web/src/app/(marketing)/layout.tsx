import type { ReactNode } from 'react'

/**
 * Route group for the public marketing site (`/` and future sibling routes).
 * Parentheses keep URLs clean; add shared layout or providers here as needed.
 */
export default function MarketingRouteGroupLayout({ children }: { children: ReactNode }) {
  return children
}
